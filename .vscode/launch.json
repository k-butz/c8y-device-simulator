{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launcher",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "envFile": "${workspaceFolder}/.env",
            "cwd": "${workspaceFolder}",
            "program": "${workspaceFolder}/main.go",
            "dlvFlags": ["--check-go-version=false"] 
        }
    ]
}